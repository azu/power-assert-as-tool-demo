{
  "name": "power-assert-as-tool-demo",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azu/power-assert-as-tool-demo.git"
  },
  "author": "azu",
  "email": "azuciao@gmail.com",
  "homepage": "https://github.com/azu/power-assert-as-tool-demo",
  "license": "MIT",
  "files": [
    "src/",
    "lib/"
  ],
  "bugs": {
    "url": "https://github.com/azu/power-assert-as-tool-demo/issues"
  },
  "version": "1.0.0",
  "description": "power-assert as a tool demo repository",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build",
    "test": "mocha"
  },
  "keywords": [
    "power-assert",
    "babel"
  ],
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-plugin-auto-import-assert": "^1.0.2",
    "babel-plugin-jsdoc-to-assert": "^1.6.0",
    "babel-plugin-unassert": "^2.0.1",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-jsdoc-to-assert": "^1.0.1",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.7.2",
    "mocha": "^2.4.5"
  }
}
